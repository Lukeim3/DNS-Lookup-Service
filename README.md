# DNS-Lookup-Service

Terminal based DNS lookup service in Java based on internet protocal RFC 1035

Uses iterative queries from root to authoritative servers via UDP/TCP sockets

Implemented TCP fallback for truncated responses